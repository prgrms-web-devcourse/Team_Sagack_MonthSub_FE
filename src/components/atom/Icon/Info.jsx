import React from 'react';
import InfoIcon from '@material-ui/icons/Info';
import styled from '@emotion/styled';

const Info = () => <StyledInfoIcon />;

export default Info;

const StyledInfoIcon = styled(InfoIcon)`
  color: inherit;
`;
