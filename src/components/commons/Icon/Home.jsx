import React from 'react';
import HomeIcon from '@material-ui/icons/Home';
import styled from '@emotion/styled';

const Home = () => <StyledHomeIcon />;

export default Home;

const StyledHomeIcon = styled(HomeIcon)`
  color: inherit;
`;
