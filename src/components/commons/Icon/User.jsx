import React from 'react';
import PersonIcon from '@material-ui/icons/Person';
import styled from '@emotion/styled';

const User = () => <StyledUserIcon />;

export default User;

const StyledUserIcon = styled(PersonIcon)`
  color: inherit;
`;
